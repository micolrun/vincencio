# Security policy

## GitHub에 올리면 안 되는 정보

- `.env`와 OAuth Client Secret
- OpenAI API 키와 GitHub 토큰
- 원본 음성 파일
- 성경·전례문 사용 승인 문서의 비공개 사본
- 생성 이미지와 완성 영상
- `data`, `backups`, `secrets`, `logs`, `renders`

비밀정보를 실수로 커밋했다면 파일만 삭제하지 말고 해당 키를 즉시 폐기·재발급해야 합니다.

## 원격 접속

8010번 포트를 공유기에서 인터넷에 공개하지 않습니다. 앱은 기본적으로 `127.0.0.1`에만 바인딩합니다. 원격 지원이 필요하면 Tailscale 같은 사설망을 사용합니다.

## 취약점 신고

공개 Issue에 API 키, 사용자 음성, OAuth 정보 또는 개인 데이터를 첨부하지 않습니다. 저장소 소유자에게 비공개로 전달합니다.

